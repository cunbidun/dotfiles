#!/bin/sh

case $BLOCK_BUTTON in
	6) "$TERMINAL" -e "$EDITOR" "$0" ;;
esac

if [ "$(pamixer --get-mute)" = true ]; then
	icon="^c#BF616A^"
fi

vol="$(pamixer --get-volume)"

echo "^c#81A1C1^VOL: ^d^$icon$vol%^d^"
