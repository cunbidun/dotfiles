#!/usr/bin/env python3
import requests
from decimal import *

res = requests.get(
    "https://sochain.com//api/v2/get_price/DOGE/USD").json()['data']

dogePrice = Decimal(res['prices'][1]['price'])

getcontext().prec = 7
print(dogePrice * Decimal(100))

