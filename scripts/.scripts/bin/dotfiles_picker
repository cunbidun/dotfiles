#!/bin/bash

DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" >/dev/null 2>&1 && pwd)"
dotfiles="$DIR/dotfiles.txt"

path=$(grep -v "^\#" $dotfiles | grep . | dmenu -i -l 10 -p "select dotfile: ")
if [ -n "$path" ]; then
	"$TERMINAL" -e "$EDITOR" "$path"
fi
