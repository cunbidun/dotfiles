#!/usr/bin/env python3
from decimal import Decimal, getcontext

import requests

res = requests.get("https://sochain.com//api/v2/get_price/DOGE/USD").json()["data"]

dogePrice = Decimal(res["prices"][1]["price"])

getcontext().prec = 7
print(f"^c#81A1C1^DOGE: ^d^{dogePrice * Decimal(100)}")
