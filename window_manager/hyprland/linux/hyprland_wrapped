#!/bin/sh

cd $HOME

export PICKER=tofi
export TERMINAL=alacritty

yay -R xremap-x11-bin
yay -S xremap-wlroots-bin --needned

pkill waybarwrapped
pkill .waybar-wrapped
pkill .pypr-wrapped

exec Hyprland
