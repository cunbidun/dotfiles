typeset -U path cdpath fpath manpath
for profile in ${(z)NIX_PROFILES}; do
  fpath+=($profile/share/zsh/site-functions $profile/share/zsh/$ZSH_VERSION/functions $profile/share/zsh/vendor-completions)
done

HELPDIR="/nix/store/lrlls6c62cfix6mvgkrm2x4pyps53cb5-zsh-5.9/share/zsh/$ZSH_VERSION/help"

# Add plugin directories to PATH and fpath
plugin_dirs=(
  vi-mode
)
for plugin_dir in "${plugin_dirs[@]}"; do
  path+="/Users/cunbidun/.zsh/plugins/$plugin_dir"
  fpath+="/Users/cunbidun/.zsh/plugins/$plugin_dir"
done
unset plugin_dir plugin_dirs


source /nix/store/2zi8zhz3ibdkp95n5pd0wl4gcc096cq0-zsh-autosuggestions-0.7.1/share/zsh-autosuggestions/zsh-autosuggestions.zsh
ZSH_AUTOSUGGEST_STRATEGY=(history)
ZSH_AUTOSUGGEST_HIGHLIGHT_STYLE="fg=#808080"


# oh-my-zsh extra settings for plugins

# oh-my-zsh configuration generated by NixOS
plugins=(git)


source $ZSH/oh-my-zsh.sh

# Source plugins
plugins=(
  vi-mode/share/zsh-vi-mode/zsh-vi-mode.plugin.zsh
)
for plugin in "${plugins[@]}"; do
  [[ -f "/Users/cunbidun/.zsh/plugins/$plugin" ]] && source "/Users/cunbidun/.zsh/plugins/$plugin"
done
unset plugin plugins

# History options should be set in .zshrc and after oh-my-zsh sourcing.
# See https://github.com/nix-community/home-manager/issues/177.
HISTSIZE="10000"
SAVEHIST="10000"

HISTFILE="/Users/cunbidun/.zsh_history"
mkdir -p "$(dirname "$HISTFILE")"

if [[ $options[zle] = on ]]; then
  source <(/nix/store/fj9vnxvfa248wb0x6ybcmx3axpw77llb-fzf-0.67.0/bin/fzf --zsh)
fi

# Set shell options
set_opts=(
  HIST_FCNTL_LOCK HIST_IGNORE_DUPS HIST_IGNORE_SPACE SHARE_HISTORY
  NO_APPEND_HISTORY NO_EXTENDED_HISTORY NO_HIST_EXPIRE_DUPS_FIRST
  NO_HIST_FIND_NO_DUPS NO_HIST_IGNORE_ALL_DUPS NO_HIST_SAVE_NO_DUPS
)
for opt in "${set_opts[@]}"; do
  setopt "$opt"
done
unset opt set_opts

ZVM_LINE_INIT_MODE=$ZVM_MODE_INSERT

[ -f "$HOME/.cargo/env" ] && . "$HOME/.cargo/env"
[ -d "/snap/bin" ] && export PATH=$PATH:/snap/bin

if [ ! -d "/etc/nixos" ]; then
  export CONDA_CHANGEPS1=false
  export PATH=/etc/profiles/per-user/$USER/bin:$PATH
fi

# Use vim keys in tab complete menu:
bindkey -M menuselect 'h' vi-backward-char
bindkey -M menuselect 'k' vi-up-line-or-history
bindkey -M menuselect 'l' vi-forward-char
bindkey -M menuselect 'j' vi-down-line-or-history

# adding important bin to bash
export PATH=$PATH:$HOME/.local/bin
export PATH=$PATH:$HOME/.cargo/bin

eval "$(starship init zsh)"
eval "$(atuin init zsh --disable-up-arrow)"


if [[ $TERM != "dumb" ]]; then
  eval "$(/nix/store/602d77lalcmm01xr29ahlhlz68z1ysvz-starship-1.24.2/bin/starship init zsh)"
fi

if [[ $options[zle] = on ]]; then
  eval "$(/nix/store/7940hh7r810w33q49lsl3a1p3dpd870b-atuin-18.11.0/bin/atuin init zsh )"
fi

alias -- CP='cd $HOME/competitive_programming/'
alias -- cat=bat
alias -- cdnote='cd $HOME/note'
alias -- la='eza -a'
alias -- ll='eza -l'
alias -- lla='eza -la'
alias -- ls='exa -la'
alias -- lt='eza --tree'
alias -- s='source $HOME/.zshrc'
alias -- tree='tree -a'
source /nix/store/s0cv7fwyfwji191ipk68x2850slnz7yx-zsh-syntax-highlighting-0.8.0/share/zsh-syntax-highlighting/zsh-syntax-highlighting.zsh
ZSH_HIGHLIGHT_HIGHLIGHTERS=(main)


